struct {{- ' {}'.format(type_name) if type_name else '' }} {
    {%- for member in member_list %}
    {{ member | indent(4, False) }}
    {%- endfor %}
}{{ ' ' + struct.name if struct.name else '' }};